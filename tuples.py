#Immutable (can't add/change)
#Useful for fixed data
#Faster than Lists

#Constructors - creating a new tuples

x  = ()
x = (1,2,3)
x = 1,2,3
x = 2, # the comma tells Python it's a tuple and not a 1 item integer
print(x, type(x))
#output (2,) <class 'tuple'>